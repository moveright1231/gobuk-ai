# Notion 내부 통합 토큰
#   1) https://www.notion.so/my-integrations 에서 발급
#   2) 각 DB를 풀페이지로 연 뒤 ... > 연결 > 통합 추가 로 권한을 줘야 함
#      (이걸 빼먹으면 쿼리는 성공하는데 결과가 0건으로 나옴)
NOTION_TOKEN=secret_xxxxxxxxxxxx
NOTION_VERSION=2025-09-03

# 가이드 위키의 데이터소스 ID.
#   노션 URL 에 보이는 건 '데이터베이스 ID' 라서 그대로 쓰면 404 가 난다.
#   위 토큰을 채우고 `python sync.py --discover` 를 돌리면 붙여넣을 값이 나온다.
#   워크스페이스마다 값이 다르므로 코드에 박지 않는다.
WIKI_DS_ID=

# 위키의 제목 프로퍼티명. 원본과 다르면 여기서 바꾼다.
WIKI_TITLE_PROP=페이지
# 게시 여부를 담은 프로퍼티명. 비워두면 수집된 행을 전부 노출한다.
#   노션에 '상태' 셀렉트(작성중/검수중/게시)를 추가했다면 상태 로 채운다.
WIKI_STATUS_PROP=

# 임베딩용
OPENAI_API_KEY=sk-xxxxxxxxxxxx
EMBED_MODEL=text-embedding-3-small

# 디스코드 봇
#   Bot > Reset Token 으로 발급
#   Bot > Privileged Gateway Intents > MESSAGE CONTENT INTENT 를 켜야 함
DISCORD_TOKEN=
# 멘션 없이도 반응할 채널 ID. 쉼표로 여러 개. 비우면 /거북 명령만 동작한다.
BOT_CHANNEL_IDS=

# 인사/농담/일반상식에 가볍게 답하는 잡담 경로. 0 으로 끌 수 있다.
# 게임 질문은 이 경로로 가지 않는다 (answer.py 의 두 겹 방어 참고).
SMALLTALK=1

# 로컬 저장소 경로
GOBUK_DB_PATH=./gobuk.sqlite3
