# 프로젝트 노트

거북AI — 마인크래프트 서버 '거북스토리'의 디스코드 질문응답 봇.
Notion에 정리된 게임 정보를 읽어 유저 질문에 답한다.

## 지금 상태

A단계(Notion 동기화)와 B단계(라우터/메모리뱅크/봇) 구현 완료. 테스트 전부 통과.
**아직 실서비스 투입 전이며, 임계값 튜닝과 DB 확충이 남았다.**

2026-09-01: Notion이 **위키 한 장으로 통합**됐다. 정형 DB 4종(직업/아이템/
레시피/가이드)은 사라지고 `거북 스토리 가이드_ai` 단일 DB 21건이 됐다.
데이터는 이제 샘플이 아니라 실제 서버 내용이다.

구조가 바뀐 결과 **정보가 프로퍼티에 없고 전부 페이지 본문에 있다.** 프로퍼티는
`태그`(서버 기초 가이드/생활직업/전투 시스템/전투 직업)와 제목뿐이고 `상태`도 없다.
그래서 `exact`(LLM 0회)의 비중이 크게 줄고 답변이 `direct`/`llm`으로 옮겨갔다 —
**호출당 비용이 올라갔다.** 임계값 튜닝이 전보다 중요해졌다.

## 실행

```bash
pip install -r requirements.txt
cp .env.example .env          # 토큰 채우기

python sync.py --doctor       # 설정/권한 점검. 뭔가 안 되면 항상 여기부터
python sync.py                # 증분 동기화 (크론 15분)
python sync.py --full         # 전체 + 삭제 정합 (크론 1일 1회)
python sync.py --reflatten    # API 없이 로컬 재생성 (답변 문구 수정 시)
python sync.py --stats

python query.py "토스파 레시피 알려줘"
python query.py --bench       # 표준 질문 12개 일괄 실행, 경로 분포 출력
python query.py -i
python query.py --unanswered  # 답변 못 한 질문 (= 문서 작성 우선순위)
python bot.py

python tests/test_sync.py          # 동기화 로직 (API 불필요)
python tests/test_router.py         # 라우터 + 메모리뱅크 (API 불필요)
```

**코드를 고쳤으면 두 테스트를 반드시 돌린다.** 둘 다 외부 API 없이 동작한다.

## 디렉토리 구조

```
sync.py  query.py  bot.py     진입점 (얇은 래퍼. 명령어는 예전과 같다)
gobuk/
  config.py                   .env 로딩, 데이터소스 등록부, 임계값, 프롬프트
  notion/client.py            Notion REST 래퍼. 레이트리밋/페이지네이션/표 파싱
  sync/
    flatten.py                원시 행 -> answer_text / search_text, 본문 청킹
    pipeline.py               fetch -> flatten -> ripple -> reconcile
    doctor.py                 --doctor / --discover (설치 진단)
    cli.py                    argparse + 적재 상태 출력
  store/
    base.py                   연결. DDL 은 각 믹스인이 들고 있다
    records.py                레코드/별칭/동기화 커서/변경 로그
    search.py                 청크/임베딩/FTS
    cache.py                  메모리뱅크 (표 정의 + 클래스)
    unanswered.py             답변 못 한 질문 로그
  engine/
    intent.py                 의도 판별 + 고유명사 추출
    answer.py                 응답 라우팅
    embed.py                  OpenAI 임베딩
    cli.py                    query.py 구현
  bot.py                      디스코드 클라이언트
tests/
  test_sync.py                동기화 로직 (API 불필요)
  test_router.py              라우터 + 메모리뱅크 (API 불필요)
```

`Store` 는 관심사별 믹스인을 합친 것이다(`store/__init__.py`). 클래스를 쪼개지
않은 이유는 `delete_missing`(레코드)이 `_drop_chunks`(검색)를 부르는 것처럼
관심사를 가로지르는 연산이 실제로 있고, 위임으로 풀면 배선만 늘기 때문이다.
표 정의는 각 믹스인 모듈이 자기 것을 들고 있다 — 예전에는 `memory_bank` 표만
`store.py` 에 있어서 캐시를 고칠 때 두 파일을 왕복해야 했다.

## 파일 맵

| 파일 | 역할 |
|---|---|
| `gobuk/config.py` | 데이터소스 등록부, 의도 키워드, 임계값, 프롬프트 |
| `gobuk/notion/client.py` | Notion REST 래퍼. 레이트리밋, 페이지네이션, 본문 수집 |
| `gobuk/sync/flatten.py` | 원시 행 → `answer_text`(완성 답변) / `search_text`(임베딩용) |
| `gobuk/store/` | SQLite 전부. 레코드, 별칭 색인, 청크, 변경 로그, 캐시, 미답변 질문 |
| `gobuk/engine/embed.py` | OpenAI 임베딩 (변경분만) |
| `sync.py` | 동기화 오케스트레이션 + `--doctor` |
| `gobuk/engine/intent.py` | 의도 판별 + 고유명사 추출 |
| `gobuk/store/cache.py` | 시맨틱 캐시 |
| `gobuk/engine/answer.py` | 응답 엔진 (라우팅) |
| `gobuk/bot.py` | 디스코드 봇 |
| `query.py` | 엔진 CLI. 봇과 같은 경로를 탄다 |

## 응답 경로

위에서부터 시도. 위쪽일수록 싸고 빠르고 정확하다.

| 경로 | 조건 | LLM |
|---|---|---|
| `cache` | 메모리뱅크 히트 | 0회 |
| `exact` | 고유명사 정확매칭 → 저장된 완성 답변 | 0회 |
| `direct` | 벡터 상위 결과가 `VECTOR_DIRECT` 이상 | 0회 |
| `llm` | 여러 문서를 엮어야 함 | 1회 |
| `chat` | 게임 질문이 아님 (인사/농담/일반상식) | 1회 |
| `fallback` | 근거 없음 → 관리자 문의 안내 | 0회 |

정형 질문(레시피/아이템/직업)은 대부분 `exact`에서 끝난다.
**이게 토큰 절감의 핵심이고, 메모리뱅크는 그 위에서 비정형 질문의 반복을 걷어내는 보조 장치다.**

## Notion 데이터소스

`config.DATA_SOURCES`에 등록되어 있다. 데이터베이스 ID가 아니라 **데이터소스 ID**다
(2025-09-03 API부터 쿼리가 `/v1/data_sources/{id}/query`로 옮겨갔고 둘은 호환되지 않는다).

| key | 라벨 | 상태 |
|---|---|---|
| `wiki` | 위키 | **운영 중.** 21건. 실제 ds_id 는 `gobuk/config.py` 참고 |
| `job` / `item` / `recipe` / `guide` | 직업/아이템/레시피/가이드 | `ds_id: None`. 노션에 없음 |

`ds_id` 가 `None` 이면 동기화·`--doctor`·삭제정합에서 모두 건너뛴다. 등록부와
빌더(`flatten.BUILDERS`), 테스트는 남겨뒀다 — 재료·가격·요구레벨처럼 값이 확정된
정보는 본문 RAG 보다 프로퍼티 조립이 정확하고 LLM 호출이 0회다. 기획자가 정형 DB를
다시 만들면 `ds_id` 만 채우면 되살아난다.

**게시 판정은 DB마다 다르다.** `status_prop` 에 적힌 프로퍼티를 보고, `None` 이면
그 DB는 전부 게시로 취급한다. 위키에는 `상태` 프로퍼티가 없어서 `None` 이다 —
그대로 `"상태"` 를 읽게 두면 전 행이 미게시로 걸러져 **수집은 되는데 봇만 0건으로
답하는**, 원인 찾기 제일 어려운 실패가 난다.

## 건드리면 안 되는 설계 결정

아래는 전부 구체적인 실패를 막으려고 그렇게 만든 것이다. 바꾸기 전에 이유를 먼저 읽을 것.

### 1. Notion `상태` 필터를 서버 쪽에 걸지 않는다

`query_rows()`는 모든 상태의 행을 받아온다. 서버에서 `상태=게시`로 거르면
**`게시 → 검수중`으로 내려간 행이 쿼리에 안 잡혀 로컬에 계속 남는다.**
전부 받아서 로컬에서 판단한다.

### 2. 삭제는 `--full`에서만 잡힌다

지워진 페이지는 애초에 쿼리 결과에 없다. 증분으로는 절대 못 잡는다.
`reconcile_stage()`의 전체 ID 대조가 유일한 방법이다.

### 3. 참조 전파 (`ripple_stage`)

아이템 `토마토`의 이름을 바꾸면 그걸 재료로 쓰는 레시피의 `answer_text`가 낡는다.
그런데 **레시피 행은 수정된 적이 없어서 `last_edited_time` 증분에 안 잡힌다.**
`depends_on`에 참조 페이지 ID를 저장해두고 역추적한다. API 재호출은 없다.

### 4. `raw_props` 보관

답변 포맷을 바꿀 때 Notion을 다시 긁지 않아도 되게 원본 프로퍼티를 저장한다.
`--reflatten` 한 방으로 전체 재생성. 전체 재수집은 레이트리밋 때문에 오래 걸린다.

### 5. 메모리뱅크는 유사도만 믿지 않는다 — 가장 중요

이 도메인에서 임베딩 유사도만으로 캐시를 히트시키면 **반드시 사고가 난다.**

```
"요리사 토마토스파게티 레시피"  vs  "요리사 크림스파게티 레시피"   → 0.96+
"팔라딘 전직 조건"             vs  "클레릭 전직 조건"            → 0.97+
```

문장 구조가 같고 고유명사 한 단어만 다르기 때문이다. 그래서 캐시 히트에
**고유명사 집합(`entity_key`) 완전 일치**를 필수 조건으로 건다.
`entity_key`는 별칭 사전으로 찾은 **페이지 ID**를 정렬해 이어붙인 값이라,
표기가 달라도(`토스파` vs `토마토 스파게티`) 같은 페이지면 같은 키가 된다.

의도도 함께 본다. `치즈 레시피`와 `치즈 얼마`는 고유명사가 같아도 답이 달라야 한다.

`tests/test_router.py` 4번 항목이 이걸 지킨다. **이 테스트를 우회하거나 완화하지 말 것.**

### 6. LLM의 거절은 낱말이 아니라 표식(`config.DECLINE`)으로 받는다

모델이 "모르겠습니다"라고 자연어로 거절하게 두면 정상 답변과 구분할 수가 없다.
실제로 `"없습니다"` 를 거절 신호로 쓰다가, **문서에 근거한 정상 답변인
"요구 레벨이 없습니다" 가 통째로 버려지고 관리자 문의가 나가는 버그**가 있었다.

지금은 두 프롬프트 모두 답할 수 없을 때 `__NO__` 만 출력하도록 지시한다.
표식은 정상 답변과 절대 겹치지 않는다. 모델이 표식을 무시하고 말로 거절하는
경우만 좁은 낱말 검사(`모르겠`/`정보가없`/`찾을수없`/`알수없`)로 한 번 더 잡는다.
`없습니다` 는 그 목록에서 뺐다. `tests/test_router.py` 11번이 이걸 지킨다.

### 7. 잡담 경로는 게임 질문을 절대 받지 않는다

인사/농담/일반상식까지 "관리자에게 문의하세요"로 보내면 봇이 쓸데없이 딱딱해진다.
그래서 `chat` 경로를 뒀는데, **여기로 게임 질문이 새면 LLM이 지어낸다.**
그게 이 프로젝트에서 제일 큰 사고다. 두 겹으로 막는다.

1. **고유명사나 의도가 잡힌 질문은 애초에 안 보낸다** (`answer.ask`).
   그건 "문서가 없는 게임 질문"이므로 관리자 문의가 맞다.
   `구하림은 누구야` 가 282ms 만에 관리자 문의로 끝나는 게 이 경로다 (LLM 미호출).
2. 그래도 게임 정보를 물으면 모델이 `__NO__` 를 내도록 프롬프트에 박아뒀다.
   표식이 나오면 `_smalltalk` 이 `None` 을 반환해 원래대로 관리자 문의가 나간다.

**서버 자체에 대한 설명(컨셉·규칙·콘텐츠·분위기)도 금지 대상이다.**
`이 서버 어떤 서버야` 는 가이드 문서로 답해야 할 질문이지 LLM이 지어낼 것이 아니다.
막아두면 미답변 로그에 쌓여 기획자에게 "이 문서가 필요하다"고 넘어간다.

잡담은 캐시하지 않는다. 같은 농담만 반복하는 봇이 된다.

### 8. 벡터DB를 쓰지 않는다

수천 행 규모에서 numpy 전수 코사인은 수 ms에 끝난다. Chroma/Qdrant를 붙이면
운영 대상만 늘어난다. 수만 행을 넘어가면 `chunks` 테이블만 옮기면 되고
나머지 코드는 그대로다. **미리 붙이지 말 것.**

### 9. 한국어 FTS는 trigram

기본 `unicode61` 토크나이저는 조사 때문에 한국어에서 잘 안 맞는다.
`chunks_fts`는 `tokenize='trigram'`을 쓴다 (SQLite 3.34+ 필요).

### 10. SQLite `check_same_thread=False`

봇이 이벤트 루프를 막지 않으려고 엔진을 `asyncio.to_thread`로 호출한다.
동시 접근은 `gobuk/bot.py`의 `asyncio.Lock`으로 막는다. **락을 제거하지 말 것.**

## 코딩 규칙

- 주석과 문서는 한국어. 사용자(기획자·운영자)가 읽는다.
- 주석은 **왜 그렇게 했는지**를 쓴다. 무엇을 하는지는 코드가 말한다.
- 새 DB 추가: `config.DATA_SOURCES`에 항목 추가 → `gobuk/sync/flatten.py`에 `build_*` 빌더 작성 →
  `BUILDERS`에 등록. 그 외 파일은 손댈 필요 없다.
- 에러 메시지는 **다음에 뭘 해야 하는지**까지 적는다. `--doctor`가 그 예다.
- 외부 API 호출은 재시도 가능/불가능을 반드시 구분한다.
  (OpenAI는 크레딧 소진에도 429를 준다. 재시도하면 안 된다 — `embed.FATAL_CODES`)

## 남은 작업

우선순위 순.

1. **임계값 튜닝** — `.env`의 `VECTOR_MIN` / `VECTOR_DIRECT` / `CACHE_SIM`은
   실제 질문 로그가 없는 상태의 임시값이다. 지금 값으로 열면 품질이 안 나온다.
   초기엔 `VECTOR_MIN`을 높게 잡아 **틀린 답보다 "모르겠다"가 나오게** 한다.
   게임 정보는 틀리면 유저가 바로 알아채고 신뢰가 무너진다.
   → 근거가 될 로그는 아래 미답변 적재로 쌓이기 시작한다. 봇을 얼마간 돌린 뒤
   `--unanswered`와 `--bench`의 유사도 분포를 같이 보고 조정한다.

2. **나머지 DB** — 지역 / NPC / 퀘스트 / 몬스터 / 스킬 / 용어사전.
   스키마는 README 초안에 있다. 용어사전은 미리 채우는 게 아니라
   실제 질문 로그를 보고 쌓아가는 DB다.

3. **`INTENT_RULES` 확충** — 실제 유저 어휘를 보고 계속 채워야 한다.
   미답변 로그에서 `의도=None`으로 자주 찍히는 질문이 곧 후보다.

4. 조사 처리가 규칙 기반이라 완벽하지 않다. 누락이 잦으면 `kiwipiepy` 고려.
   다만 별칭 사전이 상당 부분 보완하므로 급하지 않다.

### 완료됨

- **답변 못 한 질문 로그** — `fallback`으로 빠진 질문이 `unanswered` 테이블에
  쌓인다. 같은 질문은 행이 늘지 않고 `asked_count`만 오르므로, **많이 물어본
  순서가 곧 문서 작성 우선순위**다. `python query.py --unanswered`로 뽑아
  기획자에게 그대로 넘기면 된다. `sync.py --stats`에도 상위 3건이 나온다.

  읽는 법: `고유명사`가 잡혔는데 답을 못 했다면 **문서는 있는데 내용이 비었다는
  뜻**이고, 안 잡혔다면 아예 없는 문서다. 전자가 훨씬 고치기 쉽다.

  임베딩 실패 같은 설비 장애는 적재하지 않는다(문서 부재가 아니므로).
  `--bench`는 같은 질문을 반복 실행하므로 집계를 오염시키지 않게 꺼져 있다.

## 기획자에게 전달된 작성 규칙

문서 품질이 봇 품질을 그대로 결정한다. 아래가 지켜지지 않으면 코드로 못 메운다.

- 정보는 프로퍼티에, 설명은 본문에
- 1행 = 1개체 ("모든 직업 정리" 같은 페이지 금지)
- 별칭을 최대한 많이 (유저는 "토스파"라고 묻지 "토마토 스파게티"라고 안 쓴다)
- 스크린샷에 정보 담지 말 것 (봇이 못 읽는다)
- 본문에 표 그리지 말 것 (표로 정리할 정보면 그건 DB다)
- 재료는 텍스트가 아니라 relation + 수량 프로퍼티로
- 수치는 숫자 프로퍼티로 ("레벨 좀 높아야 함" ❌ / `요구레벨: 15` ✅)
- 패치로 값이 바뀌면 행을 수정하고 새로 만들지 않기

## 알려진 한계

- Notion `relation`이 25개를 넘으면 잘려서 온다. 현 스키마(재료 슬롯 1개씩)에서는
  안 걸리지만, 다대다 관계를 추가하면 `__TRUNCATED__` 마커를 확인해야 한다.
- 본문 표 블록은 마크다운 표로 펴서 읽는다 (표당 API 호출 1회 추가).
- `detect_intent`가 단순 부분문자열 검사라 오탐이 난다.
  (`구하림은 누구야` → `구하`가 걸려 `의도=item`)
  지금은 경로만 나빠질 뿐 틀린 답이 나가지는 않아 급하지 않다.
  **NPC/지역 DB를 추가할 때 같이 볼 것** — 이름이 별칭 색인에 오르면
  잘못 잡힌 의도가 `_exact`에서 정답 후보를 걸러낸다.
- 이미지 안의 텍스트는 읽지 못한다.

## 서버 소개 문서 (base.md)

노션 위키에 없는 '서버 자체'에 대한 설명은 저장소의 `base.md` 에 있다.
장르·재화(쉘)·월드 구조(드라멜/마을/야생)·전투 성장 흐름·단축키·규칙 같은 전제다.

`sync.py` 가 위키와 같은 방식으로 적재한다(`sync.pipeline.local_stage`). 노션이
아니라 로컬 파일이므로 `ds_id` 가 없고 API 호출도 없다. **파일을 고쳤으면
`python sync.py --reflatten` 을 돌려야 봇에 반영된다.**

이 파일이 없으면 "이 서버 뭐하는 서버야", "맵은 어떤게 있지" 류가 전부 관리자
문의로 빠진다 — 위키 21개 문서는 개별 콘텐츠만 다루기 때문이다.

프롬프트에는 전문을 넣지 않는다. 약 4~5천 토큰이라 LLM 호출마다 요금이 붙는다.
`config.SERVER_BRIEF` 3~4줄만 상주시키고 본문은 검색되게 둔다.

작성 시 주의:
- 절 제목(`##`, `###`)을 달 것. 청킹 단위이며 검색 정확도를 좌우한다.
- 고유명사를 본문에도 쓸 것. 제목에만 있으면 임베딩 신호가 희석된다
  (클레릭 설명에 '클레릭'이 없어서 유사도가 0.20까지 떨어진 적이 있다).
- 유저 어휘를 함께 쓸 것. 문서가 '월드'라고만 쓰면 "맵은 어떤게 있지"에 못 답한다.
- 리워크·미확정 콘텐츠는 설명하지 말고 **상태만** 적을 것.
  봇이 바뀔 정보를 확정처럼 답하는 것이 못 답하는 것보다 나쁘다.
